services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080

  # Phase 2: add a `db` service (Postgres) here and set
  # ConnectionStrings__Postgres on `api`, with depends_on + healthcheck.
